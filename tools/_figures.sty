\ProvidesPackage{_figures}

\newif\if@figs@full@is   \@figs@full@isfalse
\DeclareOption{full}   {\@figs@full@istrue}
\DeclareOption{draft}   {\PassOptionsToPackage{draft}{graphicx}}

\ProcessOptions\relax

\usepackage{graphicx} 
\usepackage{graphbox} 
\usepackage[labelfont=bf]{caption}
% \captionsetup{belowskip=-\baselineskip}

\if@figs@full@is
    \usepackage{import}
    \usepackage{pdfpages}

    \usepackage{float}

    \usepackage{tikz, calc}
    \usetikzlibrary{arrows}
    \usetikzlibrary{cd}

    \newcommand{\incfig}[2][1]{%
        \fontsize{8}{9}\selectfont
        \def\svgwidth{#1\columnwidth}
        \import{./figs/}{#2.pdf_tex}
        \fontsize{8}{11}\selectfont
    }
\fi

\numberwithin{figure}{section}

\graphicspath{ {./images/}{./pages/} }

% venn diagram:
%https://texample.net/media/tikz/examples/TEX/venn-diagram.tex



%%%% Venn diagram defs

\def\firstcircle{(0,0) circle (1.5cm)}
\def\secondcircle{(0:2cm) circle (1.5cm)}
\def\thirdcircle{(45:2cm) circle (1.5cm)}

\def\firstcenter{(0,0)}
\def\secondcenter{(0:2cm)}
\def\thirdcenter{(45:2cm)}

\tikzset{
    filled/.style={fill=circle area, draw=circle edge, thick},
    outline/.style={draw=circle edge, thick},
    vertex/.style={
        circle, 
        minimum size=.1cm, 
        inner sep=0pt, 
        fill=black, 
        draw=circle edge, 
    },
    arrow/.style={
        ->,
        >=latex,
        draw=circle edge, 
    },
}
\colorlet{circle edge}{black}
\colorlet{circle area}{black!20}
