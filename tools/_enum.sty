\ProvidesPackage{_enum}
\ProcessOptions\relax

\usepackage[inline, shortlabels]{enumitem}

\setlist[enumerate]{
    label=(\roman*), 
    topsep=0pt, 
    itemsep=0pt, 
}
\setlist[description]{topsep=0pt, itemsep=0pt}
\setlist[itemize]{topsep=0pt, itemsep=0pt, parsep=5pt}
